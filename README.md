# 할일장

시간대(오전/오후/저녁)별로 할 일을 정리하는 종이 다이어리 감성의 할 일 관리 웹앱입니다. 순수 HTML/CSS/JS로 만들어져 별도 빌드 없이 바로 실행됩니다.

## 로컬에서 열어보기

`index.html` 파일을 브라우저로 더블클릭해서 열면 바로 동작합니다. 작성한 할 일은 브라우저의 로컬 저장소(localStorage)에 저장되어, 같은 브라우저에서 다시 열어도 유지됩니다.

## 배포하기 (GitHub Pages)

아래 안내는 대화창에 적힌 단계와 동일합니다.
